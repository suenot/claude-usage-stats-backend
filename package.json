{
  "name": "@claude-stats/backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm --prefix ../core run build && npm run build && node --test test/*.test.mjs",
    "dev": "npx tsx --watch src/server.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@claude-stats/core": "*",
    "@hono/node-server": "^2.0.12",
    "hono": "^4.12.33",
    "jose": "^6.2.5",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.20.0",
    "typescript": "^5.7.0"
  }
}
